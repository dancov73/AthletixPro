import React from 'react';
import { Box, Typography } from '@mui/material';

const AthleteMonitor = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Athlete Monitor</Typography>
      {/* Add your athlete monitor content here */}
    </Box>
  );
};

export default AthleteMonitor;
